services:
  - type: web
    name: europa-clinic
    env: static
    buildCommand: echo "Static site — no build needed"
    staticPublishPath: .
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
